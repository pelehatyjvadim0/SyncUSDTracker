# USD Tracker Service (Django)

Тестовое задание: микросервис для мониторинга актуального курса USD к RUB с историей последних запросов и встроенным ограничением частоты обновлений.

## 📋 Подзадачи 

Проект был разбит на следующие логические этапы:

### 1. Подготовка и инфраструктура
- [x] Развертывание виртуального окружения и установка зависимостей (Django, Requests).
- [x] Инициализация Django-проекта и приложения `rates`.
- [x] Настройка базовых параметров в `settings.py`.

### 2. Слой данных 
- [x] Проектирование модели `CurrencyRequest` для хранения истории.
- [x] Создание и применение миграций в SQLite.

### 3. Бизнес-логика 
- [x] Интеграция с внешним API (ЦБ РФ).
- [x] Реализация валидации временного интервала (10-секундное ограничение).
- [x] Логика сохранения данных и формирования выборки (Last 10).

### 4. Интерфейс 
- [x] Создание JSON-эндпоинта.
- [x] Обработка HTTP-статусов (200 OK, 429 Too Many Requests, 500 Error).
- [x] Настройка маршрутизации (URLs).

### 5. Документирование и QA
- [x] Ручное тестирование сценария "флуда" запросами.
- [x] Подготовка вопросов для стейкхолдеров по ТЗ.
- [x] Оформление документации (README, requirements.txt).

## 🛠 Технологический стек
* **Framework:** Django 6.0
* **Language:** Python 3.12
* **Database:** SQLite (для простоты развертывания MVP)
* **API Integration:** Синхронные запросы к ЦБ РФ (cbr-xml-daily.ru)
* **Architecture:** Service Layer (бизнес-логика вынесена из View в отдельные сервисы)

---

## 🚀 Быстрый запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/pelehatyjvadim0/SyncUSDTracker
   cd usd_tracker

    Настройте виртуальное окружение:
    Bash

    python -m venv venv
    source venv/bin/activate  # Для Linux/macOS
    # venv\Scripts\activate   # Для Windows

    Установите зависимости:
    Bash

    pip install -r requirements.txt

    Выполните миграции БД:
    Bash

    python manage.py migrate

    Запустите сервер:
    Bash

    python manage.py runserver

Эндпоинт: http://127.0.0.1:8000/get-current-usd/

🧩 Архитектурные решения

    Service Layer (rates/services.py): Вся логика работы с внешним API и проверка 10-секундного интервала инкапсулирована в класс CurrencyService. Это делает код чистым, а логику — легко тестируемой.

    Throttling: Реализовано ограничение на уровне бизнес-логики: если с момента последней записи в БД прошло менее 10 секунд, сервис возвращает ошибку 429, не нагружая внешнее API.

    DecimalField: Для хранения курсов валют использован тип Decimal, что исключает ошибки округления, характерные для Float.

❓ Вопросы к стейкхолдерам (из ТЗ)

К Продакт-менеджеру:

    Активность и нагрузка: Каковы планы по росту аудитории? Нужно ли закладывать архитектуру под тысячи одновременных запросов в секунду?

    UX при ограничениях: Допустимо ли возвращать ошибку 429 конечному пользователю? Возможно, стоит внедрить кэширование и отдавать "последний известный курс" из базы, чтобы скрыть задержку парсинга?

    Источники данных: Насколько критична точность? Сейчас используется API ЦБ РФ (обновление раз в сутки). Требуется ли переход на биржевые API (Real-time) для коммерческого использования?

К Team Lead:

    Асинхронность: Учитывая сетевые задержки внешних API, стоит ли перевести сервис на асинхронные рельсы (httpx + async views) для оптимизации ресурсов сервера?

    Инфраструктура кэширования: Планируется ли внедрение Redis? Это идеальный инструмент для реализации Throttling и временного хранения текущего курса вне основной БД.

    Фоновые задачи: Стоит ли вынести парсинг во внешний воркер (Celery), чтобы полностью изолировать HTTP-потоки от работы с внешними ресурсами?

📈 Оценка времени (Тайминги)

    Инициализация проекта и проектирование БД: 
    План: 5 мин.
    Фактически: 3 мин.

    Реализация Service Layer и интеграция с API:
    План: 25 мин.
    Фактически: 10 мин.

    Настройка Views, URLs и обработка ошибок: 
    План: 40 мин. 
    Фактически: 20 мин.

    Составление README и подготовка к сдаче:
    План: 40 мин.
    Фактически: 30 мин.


    Итог:
    План: ~2 часов
    Фактически: 1 час, 3 минуты

